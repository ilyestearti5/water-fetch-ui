import { Root } from 'react-dom/client';

export declare const startApplication: (App: JSX.Element | (() => JSX.Element), Loading?: JSX.Element | (() => JSX.Element), timeLoading?: number) => Promise<Root>;

export { }
